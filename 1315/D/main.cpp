#include <bits/stdc++.h>



int main()
{
    int n;
    std::cin >> n;
    std::array<int, n> a;
    std::array<int, n> t;
    std::map<int, std::vector> map;
    for (int i = 0; i < n; ++i) {
        std::cin >> a[i];
    }
    for (int i = 0; i < n; ++i) {
        std::cin >> t[i];
    }
    for 

}
