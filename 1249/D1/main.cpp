#include <bits/stdc++.h>

std::vector<int> left(int(2e5) + 100);
std::vector<int> right(int(2e5) + 100);

int main()
{
    int n, k;
    std::cin >> n >> k;
    for (int i = 0; i < n; ++i) {
        std::cin >> left[i] >> right[i];
    }
    
}
